# pdf-lib Export Adapter Boundary

This folder is reserved for the provisional production export adapter.

Phase 5 approved `pdf-lib` only conditionally. Export code must stay behind the
`PdfExporter` port so a stronger exporter can replace it without changing domain
or presentation code.
